var db = require("../config.js");
var mongoose = require('mongoose');
var Schema = mongoose.Schema;

var cors = require('cors');
const Axios = require("axios");
const Port_folio = function (port_folio) {

};

Port_folio.mandate_normal = (email, result) => {
    console.log();
}